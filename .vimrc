" Required
set number              " Show line numbers   
set ic                  " Ignore Case, /SEARCHING
set hls                 " Highlight search
set history=500         " keep 50 lines of command line history. U can change the value
set ruler               " show the cursor position all the time
set showcmd             " display incomplete commands
set incsearch           " do incremental searching
syntax on               " Script, program syntax.
set autoindent          " set autoindenting on
set smartindent         " Set intent smartly
set autoread            " Set to auto read when a file is changed from the outside
set undolevels=500      " use many muchos levels of undo


" Optional
" set nowrap            " don't wrap lines (Uncomment if required)
" set cursorline        " Highlight current line 
